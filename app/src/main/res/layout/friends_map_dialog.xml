<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nearReportDialog"
    android:layout_width="@dimen/nearby_report_dialog_width"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="@dimen/half_of_user_image_map_dialog"
        android:divider="@drawable/vertical_divider"
        android:orientation="vertical"
        android:paddingTop="@dimen/half_of_user_image_map_dialog"
        android:showDividers="middle">

        <TextView
            android:id="@+id/userName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:padding="@dimen/icon_drawable_padding"
            android:text="Username"
            android:textColor="@android:color/black"
            android:textSize="@dimen/dialog_report_title_text_size" />

        <Button
            android:id="@+id/chatBtn"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/message_user"
            android:textSize="@dimen/report_dialog_button_textsize" />
    </LinearLayout>

    <ImageView
        android:id="@+id/userImage"
        android:layout_width="@dimen/user_image_map_dialog_height"
        android:layout_height="@dimen/user_image_map_dialog_height"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:adjustViewBounds="true"
        android:background="@color/green_color"
        android:scaleType="centerCrop" />

</RelativeLayout>